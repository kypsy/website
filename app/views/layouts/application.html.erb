<!DOCTYPE html>

<html dir="ltr" lang="en" id="<%= @slug %>">

<%= render "shared/head" %>

<body>
  <%= render "shared/header" %>

  <section role="main" class="col-md-6 offset-md-3 col-sm-12">
    <% if show_update_profile? %>
      <p class="alert alert-success">
        <%= t(:completed_profiles_cta_notice) %>
        <b><%= link_to t(:completed_profiles_cta_link_text), settings_path, class: "alert-link" %></b>
      </p>
    <% end %>

    <%= render "shared/flashes" %>

    <%= yield %>
  </section>

  <%= render "shared/footer" %>

  <%= render "shared/foot" %>
</body>
</html>
